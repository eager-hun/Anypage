<div class="sidebar-box-arrangement {{ wrapper_extra_classes | default('') }}">

    {% for item in items %}
        <div class="box-guide">
            {{ item | raw }}
        </div>
    {% endfor %}

</div>
